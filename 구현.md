## 락 전략

### order-service

Order 행은 단일 요청 스레드에서만 생성·수정된다. 동시에 같은 행을 건드리는 요청이 없으므로 **락 전략이 불필요하다.** 현재 `SQLITE_BUSY`는 SQLite 파일 수준 락 문제이며, 행 수준 락을 쓰는 DB로 교체하면 사라진다.

### inventory-service

여러 요청이 같은 `productId`의 재고 행을 동시에 읽고 차감할 수 있다. 현재는 `pool size=1`이 직렬화 역할을 대신한다.

| | 현재 (SQLite) | DB 교체 시 |
|--|--|--|
| 직렬화 수단 | `pool size=1` + WAL | `@Lock(PESSIMISTIC_WRITE)` on `findByProductId` |

- `@Lock(PESSIMISTIC_WRITE)`는 `SELECT FOR UPDATE`를 요구하는데 SQLite가 미지원이므로 **지금 추가하면 안 된다.**
- 낙관적 락(`@Version`)은 충돌을 flush 시점에 감지해 Saga 보상 트랜잭션 비용이 발생하므로, 재고 차감에는 비관적 락이 더 적합하다.

---

## Circuit Breaker 수치

### inventoryReserve (재고 차감 경로)

```yaml
sliding-window-size: 20        # 최근 20회 호출 기준으로 판단
minimum-number-of-calls: 10    # 10회 이상 쌓여야 open 판정
failure-rate-threshold: 50     # 실패율 50% 초과 시 open
slow-call-rate-threshold: 50   # 슬로우콜 비율 50% 초과 시 open
slow-call-duration-threshold: 2s
wait-duration-in-open-state: 15s
permitted-number-of-calls-in-half-open-state: 5
ignore-exceptions:
  - FeignException.NotFound    # 404: 상품 없음 → 비즈니스 오류, CB 건강도 제외
  - FeignException.Conflict    # 409: 재고 부족 → 비즈니스 오류, CB 건강도 제외
```

- **주의**: Feign `readTimeout: 1500ms` < `slow-call-duration-threshold: 2s`  
  → Feign이 먼저 TimeoutException을 던지므로, slow call로 집계되기 전에 failure로 기록된다.  
  → 실질적으로 슬로우콜 임계값은 동작하지 않고, 실패율 임계값만 유효하다.

### inventoryRelease (재고 복구 경로)

```yaml
sliding-window-size: 10        # reserve보다 작은 창 — 보상 경로는 더 빠르게 감지
minimum-number-of-calls: 5
failure-rate-threshold: 50
slow-call-rate-threshold: 50
slow-call-duration-threshold: 2s
wait-duration-in-open-state: 10s   # reserve보다 5s 짧음 — 더 빠른 회복
permitted-number-of-calls-in-half-open-state: 3
ignore-exceptions:
  - FeignException.NotFound    # 404: 이미 삭제된 상품 → 재시도 불필요
```

- release는 보상 트랜잭션 경로이므로 창 크기와 대기 시간을 reserve보다 작게 잡아 더 민감하게 반응한다.
- Retry도 함께 적용(`max-attempts: 2, wait: 500ms`)하여 일시적 네트워크 오류를 흡수한다.

---

## 토픽 네이밍

**규칙**: `{발행 서비스}.{도메인}.{이벤트}`

---

## 실패 모드

### 1. 재고 차감 실패 (reserve)

| 원인 | 처리 |
|------|------|
| CB open | `reserveFallback` → RuntimeException → Saga catch → `order.cancel()` |
| Feign timeout (1500ms) | 위와 동일 |
| 404 Not Found | `ignore-exceptions` → fallback에서 FeignException 재던짐 → Saga catch → `order.cancel()` |
| 409 Conflict (재고 부족) | 위와 동일 |

- reserve 전에는 재고 차감이 없으므로 보상 트랜잭션(release) 불필요.

### 2. 주문 상태 저장 실패 (SQLITE_BUSY)

- `saveOrderStatusWithRetry`: 최대 3회, 100ms 간격으로 재시도.
- `confirm` 저장 실패 → `order.cancel()` + `release` 보상 호출.
- `cancel` 저장 실패 → `CannotAcquireLockException` 로그 후 종료. 주문 상태가 `PENDING`으로 남는 영구 불일치 가능성이 있다.

### 3. 재고 복구 실패 (release)

```
inventoryClientFacade.release() 실패
  → releaseFallback → RuntimeException
  → "[재고 복구 실패] — 수동 복구 필요" 로그
  → 종료
```

- Retry 2회 소진 후 CB가 open되면 fallback에서 예외를 던진다.
- **재고 영구 불일치**: 차감된 재고가 복구되지 않은 채 남는다. outbox 패턴이나 dead-letter 큐 없이 로그에만 의존한다.

### 4. Kafka 발행 실패 (publishOrderConfirmed)

```
orderEventPublisher.publishOrderConfirmed() 실패
  → KafkaPublishException (브로커 미가용 / 타임아웃 / 인터럽트)
  → OrderSagaOrchestrator — KafkaPublishException 전용 catch
  → "[Kafka 발행 실패] 주문은 CONFIRMED 유지, 수동 재처리 필요" 로그
  → 보상 없음, 주문 상태 CONFIRMED 유지
```

- Kafka 발행 실패는 재고 차감·주문 확인이 모두 완료된 이후에 발생하므로 보상 트랜잭션을 실행하지 않는다.
- `KafkaPublishException`을 일반 `Exception`과 분리하여, 인프라 장애가 주문 취소로 이어지는 문제를 방지한다.
- **이벤트 유실 위험**: 발행 실패한 이벤트를 영속화하는 수단이 없어 로그에만 의존한다. 운영 환경에서는 outbox 패턴이나 dead-letter 큐를 통한 재처리 메커니즘 도입이 필요하다.
